a("data-ui-sref": "home.${property.referencedDomainClass?.propertyName}.edit({id: ${domainInstanceName}.${property?.name}.id})"){
    span("data-ng-bind": "${domainInstanceName}.${property?.name}|json"){
    }
}